function hello(param) {
    return {
        message: `This ${this.param[0].name} is out of stock`,
        buttons: [

        ]
    }
}

var varww
var ww = `Неккорректное внешнее присоединение '${связь.запрос}' в ${this.constructor.name}`;
var testProp = AAA;

var varww
varww = `Неккорректное внешнее присоединение '${связь.запрос}' в ${this.constructor.name}`;
var testProp = AAA;

var x = {
    iplocation: `Hello ${faker.address.city()}, ${faker.address.stateAbbr()}`
}