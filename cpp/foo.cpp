#include<iostream>
#include "/path/to/my/file.h"

#ifdef VAR
#define SUM(A,B) (A) + (B)

int main(int argc, char** argv)
{
	int value;
	
	string raw_str=R"(First line.\nSecond line.\nEnd of message.\n)";
	regex rr(R"(((\+|-)?[[:digit:]]+)(\.(([[:digit:]]+)?))?((e|E)((\+|-)?)[[:digit:]]+)?)");
	return 0x1.1e7f4707fc4dcp-9 + 7;
}

namespace TestSpace
{
	using Asdf.CDE;
	template <typename T>
	class CoolClass : protected BaseClass
	{
		private:
		
		static T field;
		
		public:
		
		[[deprecated]]
		foo method() const override
		{
			auto s = new Bar();
			const vector<string> s;
			
			if (s.field) {
				for(const auto & b : s.field) {
					break;
				}
			}
			int 4 = 9 + 67;
		}
		
		std::string s = "hello wordld\n";
		0x1.1e7f4707fc4dcp-9
		int number = 123'123'123Ull;
		int 4 = 9 + 67;
	}
}
void editorBug() { anotherFunct("{"); }

#endif