/// <summary>
/// This summary is now in the same color as the above comment.
/// </summary>
namespace Test
{
    class A {
        private static unsafe void LZ4compress(
            uint* hash_table,
            byte* src,
            byte* dst,
            int src_len,
            int dst_maxlen)
        {   
            var mlString = "The is my long multi-line string which I am using a backslash \
                to join over these lines."
            var 
        }
    }
}